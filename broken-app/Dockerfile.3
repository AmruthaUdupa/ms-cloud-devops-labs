# Symptom: docker ps shows it running and healthy-looking, but
# curl localhost:3000 gives "connection reset by peer".
FROM node:20-alpine
WORKDIR /app
COPY . .
ENV GREETING=hello
ENV PORT=8080
EXPOSE 3000
CMD ["node", "app.js"]
