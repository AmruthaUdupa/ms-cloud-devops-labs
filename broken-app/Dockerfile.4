# Symptom: docker ps shows nothing at all, and docker ps -a shows Exited (0).
FROM node:20-alpine
WORKDIR /app
COPY . .
ENV GREETING=hello
ENV PORT=3000
EXPOSE 3000
# shell form on purpose: the & is interpreted by sh
CMD node app.js &
